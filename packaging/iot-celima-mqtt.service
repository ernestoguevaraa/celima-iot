[Unit]
Description=Celima MQTT Integration Service
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
EnvironmentFile=/etc/default/iot-celima-mqtt
# Crea /var/lib/iot-celima-mqtt con los permisos correctos, incluso cuando el
# servicio deje de correr como root. ProtectSystem/ProtectHome/PrivateTmp
# dejan fuera cualquier otra ubicación.
StateDirectory=iot-celima-mqtt
ExecStart=/usr/local/bin/iot-celima-mqtt
Restart=on-failure
RestartSec=3

# Hardening (loosen if needed)
NoNewPrivileges=true
ProtectSystem=full
ProtectHome=true
PrivateTmp=true
ProtectControlGroups=true
ProtectKernelModules=true
ProtectKernelTunables=true
RestrictRealtime=true
LockPersonality=true

[Install]
WantedBy=multi-user.target
